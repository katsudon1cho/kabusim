# 定時実行を外部から叩く

GitHub Actions の `schedule` はベストエフォートで、**実測で最大3時間半ずれた**。

```
us-close  予定 04:15 → 実際 05:41 （ 86分遅れ）
report    予定 06:00 → 実際 07:10 （ 70分遅れ）
jp-open   予定 09:15 → 実際 12:45 （210分遅れ）
```

寄り付き後セッションが昼過ぎに、引け前セッションが引け後に走ると、
プロンプトが想定する市場の状態と実際が食い違ったまま判断することになる。

外部の定時実行サービスから `repository_dispatch` を叩けば、この遅延は無くなる。
**受け口は実装・検証済み**（送信から数秒で起動することを確認）。
あとは叩く側を用意するだけ。

外部に持たせるのは**「合図を送る」ことだけ**にしてある。どのセッションを走らせるか、
その曜日に走らせてよいか、夏時間でどうずれるかは**すべて GitHub 側で判定する**。
外部サービスに置く設定を最小にしておくと、直す場所が1箇所に集まり、
トークンを貼る回数も減る（登録するジョブは2本だけ）。

---

## 1. トークンを作る

GitHub の [Fine-grained personal access token](https://github.com/settings/personal-access-tokens/new) を作る。

| 項目 | 設定 |
|---|---|
| Repository access | **Only select repositories** → `kabusim` だけ |
| Permissions | **Contents: Read and write** のみ |
| Expiration | 1年（期限を控えておくこと） |

**他の権限は付けないこと。** これだけで `dispatches` を叩ける。
万一漏れても、影響はこのリポジトリの書き込みに限定される。

## 2. 叩く内容

エンドポイントは共通で、`event_type` だけ変える。

```
POST https://api.github.com/repos/katsudon1cho/kabusim/dispatches

ヘッダ:
  Accept: application/vnd.github+json
  Authorization: Bearer <トークン>
  X-GitHub-Api-Version: 2022-11-28
  Content-Type: application/json
```

判断セッション（**5回とも中身は同じ**）:

```json
{"event_type":"tick"}
```

価格更新:

```json
{"event_type":"prices"}
```

**どのセッションを走らせるかは外部からは指定しません。** tick を受け取った
GitHub 側が JST の「時」から決めます（session.yml の「セッション種別を決める」）。

| JST の時 | セッション |
|---|---|
| 04時 | us-close |
| 06時 | report |
| 09時 | jp-open |
| 14時 | jp-close |
| 22時 | us-open |

曜日の判定も GitHub 側で行います（日本市場ぶんは月〜金、米国の引け由来は火〜土）。
**外部サービス側で曜日を絞らないこと。** 二重に管理すると片方だけ直したときにズレます。

セッションを名指しで走らせたいとき（手動・障害復旧用）は、従来の形式も残してあります。

```json
{"event_type":"session","client_payload":{"session":"jp-open"}}
```

`session` に入れられるのは `jp-open` / `jp-close` / `us-open` / `us-close` / `report`。

動作確認用（トークンを自分の値に置き換える）:

```bash
curl -X POST https://api.github.com/repos/katsudon1cho/kabusim/dispatches -H "Accept: application/vnd.github+json" -H "Authorization: Bearer <トークン>" -H "X-GitHub-Api-Version: 2022-11-28" -d '{"event_type":"prices"}'
```

HTTP 204 が返れば成功（本文は空）。Actions タブに `repository_dispatch` の実行が現れる。

## 3. 登録するジョブ

**登録するのは2本だけです。** トークンを貼るのも2回で済みます。

サービス側のタイムゾーンを **Asia/Tokyo に設定してください。** 以下はすべて JST です。
（cron-job.org は「時」と「分」をそれぞれ選ぶ形式なので、**分は全時刻で共通**になります。
UTC 併記をやめたのはこのため。時ごとに分を変えられない以上、換算表は誤解のもとです。）

### ジョブA: セッション（`{"event_type":"tick"}`）

| 項目 | 設定 |
|---|---|
| 時 | **4, 5, 6, 9, 14, 22, 23** |
| 分 | **45** |
| 曜日 | **毎日**（絞らない。GitHub 側で判定する） |

夏時間の今、実際にセッションが走るのは 04:45 / 06:45 / 09:45 / 14:45 / 22:45 の5回です。
**5, 23時にも tick を投げているのは冬時間ぶんの席を空けてあるため**で、いまは
「該当なし」と記録して数秒で終わります。こうしておくと11月の切り替えが
コード側だけで済みます（後述）。

**分を 45 にするのは全時刻を成立させる唯一の選択です。** たとえば 05 にすると
us-open が 22:05 になり、米国市場の開場（夏時間で 22:30）より前に走ってしまいます。

| 時刻 | 市場との関係 |
|---|---|
| 04:45 | 米国の引け（05:00）15分前 |
| 06:45 | 日報。市場は関係ない |
| 09:45 | 日本の寄り付き（09:00）45分後 |
| 14:45 | 日本の引け（15:30）45分前 |
| 22:45 | 米国の開場（22:30）15分後 |

### ジョブB: 価格更新（`{"event_type":"prices"}`）

| 項目 | 設定 |
|---|---|
| 時 | **9, 10, 11, 12, 13, 14, 15** と **22, 23, 0, 1, 2, 3, 4, 5, 6** |
| 分 | **0, 20, 40** |
| 曜日 | **毎日** |

米国側を 22〜6時と広めに取ってあるのは、夏時間（22:30〜05:00）と
冬時間（23:30〜06:00）の**両方を最初から含めておく**ためです。11月に触らずに済みます。

市場が閉じている時間帯や土日は無駄打ちになりますが、価格更新は **Claude を起動しない**ので
利用枠は消費しません（GitHub Actions の実行時間だけ）。曜日を絞ろうとすると、
金曜夜の米国セッション（JST では土曜の未明）を巻き添えで落とすことになります。

### 遅延に対する余裕

tick は「時」だけで判定するので、**GitHub 側の起動が次の時刻に食い込むとセッションは
走りません。** 22:45 と 04:45 は余裕が15分と最も短い部類です。

repository_dispatch は送信から数秒で起動することを確認済みなので実害は低いですが、
**このスキップはエラーになりません。**「この時刻・曜日に該当するセッションはありません」と
記録して正常終了します。走らなかったときは Actions のログでここを見てください。

逆に、遅れて次の「時」に食い込んだ tick が別のセッションとして走る可能性もあります
（冬時間の 04:45 が 05時台にずれ込むと us-close 扱いになる、など）。これは起きても
`concurrency: kabusim-ledger` で直列化され、20時間クールダウンと1日4件の上限が効くので
台帳は壊れません。

### 夏時間

**11月に米国が冬時間へ移ると1時間後ろにずれます。直すのは `session.yml` の
時刻マッピング1箇所だけです。**

```
04) S=us-close  →  05) S=us-close
22) S=us-open   →  23) S=us-open
```

`report`（06時）と日本市場ぶんは動きません。ジョブA・ジョブBの設定も触りません
（冬時間ぶんの時刻をあらかじめ含めてあるため）。**外部サービスの管理画面を開かずに
切り替えられる**のがこの方式の利点です。

## 4. サービスの選択肢

**cron-job.org**（手軽）
無料。ブラウザのフォームだけで設定でき、デプロイ作業が要らない。
POSTボディとヘッダを指定できる。トークンは同サービスに保存される。
**ジョブの複製機能が無いので、トークンは2本ぶん手で貼ることになる**（tick 方式にした理由）。
時・分・曜日はそれぞれ独立したチェックボックスで選ぶ形式。

**Cloudflare Workers の Cron Triggers**（堅い）
無料枠で足りる。トークンを Worker のシークレットに置けるので、
第三者のフォームに貼らずに済む。実行時刻の精度も高い。
ただし Worker を1つデプロイする手間がかかる。

どちらでも構わない。**金銭が動かない実験なので、手軽さを取って問題ない。**

## 5. 切り替え後

GitHub 側の `schedule` は**残しておく**。外部サービスが止まったときの保険になる。
同じセッションが二重に走っても、`state.json` の更新は直列化されており
（`concurrency: kabusim-ledger`）、クールダウンと1日4件の上限が効くので
台帳が壊れることはない。

うまく動いているかは `usage_log.csv` の `ts` 列で分かる。
**毎時 :45 から数分以内に収まっていれば成功**（04:45 / 06:45 / 09:45 / 14:45 / 22:45）。

セッションが1回まるごと落ちているときは、`usage_log.csv` には何も残りません。
GitHub の Actions タブで `repository_dispatch` の実行を探し、
「セッション種別を決める」のログを見ること。tick は届いたが時刻が合わなかったのか、
そもそも tick が届いていないのかが区別できます。
